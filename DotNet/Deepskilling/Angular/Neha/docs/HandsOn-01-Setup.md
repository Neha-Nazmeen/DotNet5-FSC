# Hands-On 1 — Environment Setup, Project Structure & First Component

**Status:** Complete

## What was done
- Scaffolded `student-course-portal` (Angular 20, standalone components, routing, CSS).
- Documented all 8 required files in `student-course-portal/notes.txt`.
- Documented the `angular.json` production budgets (maximumWarning / maximumError) in the same file.
- Generated `HeaderComponent`, `HomeComponent`, `CourseListComponent`, `StudentProfileComponent`.
- Added portal nav links in the header and welcome/stats content on the home page.
- Wired `<app-header>` and `<router-outlet>` into `app.component.html`.

## Where
`student-course-portal/notes.txt`, `src/app/components/header/`, `src/app/pages/home/`,
`src/app/pages/course-list/`, `src/app/pages/student-profile/`.
