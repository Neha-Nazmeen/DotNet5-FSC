# features

Lazily loaded feature areas, split out of the main bundle.

- `enrollment/` — route table for the enrollment forms, loaded on demand via `loadChildren` in
  `app.routes.ts` (Hands-On 7, Step 73)
