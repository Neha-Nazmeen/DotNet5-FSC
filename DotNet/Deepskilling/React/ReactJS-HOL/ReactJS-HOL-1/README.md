# ReactJS-HOL-1: myfirstreact — Set up React & create-react-app

## Project location
`./myfirstreact/`

## How to run

```bash
cd myfirstreact
npm install
npm start
```

The app opens automatically at http://localhost:3000 (or run `npm run build` for a production build).

## Requirements
- Node.js (LTS) and npm installed — https://nodejs.org/en/download/
- Internet connection for the first `npm install` (downloads react-scripts and dependencies)

## What this covers
See the original hand-on-lab Word document (`1__ReactJS-HOL.docx`) for full objectives. This folder contains a working implementation of the task described in that document.
