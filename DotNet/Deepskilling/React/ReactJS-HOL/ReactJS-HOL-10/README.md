# ReactJS-HOL-10: officespacerentalapp — JSX, Elements, Attributes & Inline CSS

## Project location
`./officespacerentalapp/`

## How to run

```bash
cd officespacerentalapp
npm install
npm start
```

The app opens automatically at http://localhost:3000 (or run `npm run build` for a production build).

## Requirements
- Node.js (LTS) and npm installed — https://nodejs.org/en/download/
- Internet connection for the first `npm install` (downloads react-scripts and dependencies)

## What this covers
See the original hand-on-lab Word document (`10__ReactJS-HOL.docx`) for full objectives. This folder contains a working implementation of the task described in that document.
